import { useEffect, useState } from "react";
import { Intro, Status } from "../../components/common/AppUI";
import { getApiError } from "../../services/api";
import { getAdminActivities } from "../../services/platformService";
export default function ReportsPage() { const [items, setItems] = useState([]), [error, setError] = useState(""); useEffect(() => { getAdminActivities({ limit: 100 }).then(setItems).catch((requestError) => setError(getApiError(requestError))); }, []); return <><Intro eyebrow="AUDIT HISTORY" title="Administrator activity records" copy="Review important moderation and account-management actions."/>{error && <div className="form-error">{error}</div>}<section className="panel"><div className="table-wrap"><table><thead><tr><th>Action</th><th>Target</th><th>Description</th><th>Date</th></tr></thead><tbody>{items.map((item) => <tr key={item.id}><td><Status>{item.action.replaceAll("_"," ")}</Status></td><td>{item.target_type} #{item.target_id}</td><td>{item.description}</td><td>{new Date(item.created_at).toLocaleString()}</td></tr>)}</tbody></table></div></section></>; }
