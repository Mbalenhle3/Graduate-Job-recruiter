import { Status } from "../../components/common/AppUI";
const pretty = (value = "") => value.replaceAll("_", " ").replace(/\b\w/g, (c) => c.toUpperCase());
export default function OpportunityTable({ opportunities, actions }) { return <div className="table-wrap"><table><thead><tr><th>Opportunity</th><th>Type</th><th>Closing</th><th>Status</th>{actions && <th>Actions</th>}</tr></thead><tbody>{opportunities.map((item) => <tr key={item.id}><td><b>{item.title}</b><span>{item.location || "Not specified"}</span></td><td>{pretty(item.opportunity_type)}</td><td>{item.closing_date}</td><td><Status>{pretty(item.status)}</Status></td>{actions && <td><div className="row-actions">{["draft","rejected"].includes(item.status) && <button onClick={() => actions.submit(item)}>Submit</button>}<button disabled={item.status !== "published"} onClick={() => actions.close(item)}>Close</button></div></td>}</tr>)}</tbody></table></div>; }
